
# Ignore everything
*
# But descend into directories
!*/

# Allow specific extensions
!*.jl
!*.md
!*.cpp
!*.hpp

# Allow specific files
!.gitignore
!Project.toml
!.JuliaFormatter.toml
!LICENSE

# Build artifacts: generated by deps/build.jl at Pkg.build() time, contains an
# absolute path local to whoever built it - never commit.
deps/deps.jl
deps/build.log
